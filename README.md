# Swift explorations

SwiftUI mail stack prototype: interactive letter cards, page curl, SceneKit paper crumple, and gesture-driven actions (unread, archive, trash).

Open `LetterStack/LetterStack.xcodeproj` in Xcode and run on the iOS Simulator (iOS 17+).
